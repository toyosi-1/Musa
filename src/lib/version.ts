/**
 * App version and build timestamp.
 * Updated automatically on each build to force cache invalidation.
 */

export const APP_VERSION = '1.0.0';
export const BUILD_TIMESTAMP = Date.now();
export const BUILD_ID = `${APP_VERSION}-${BUILD_TIMESTAMP}`;
